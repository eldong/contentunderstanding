# Azure AI Foundry (single endpoint for OpenAI + Document Intelligence)
AZURE_AI_FOUNDRY_ENDPOINT=https://your-project.services.ai.azure.com/

# Model deployments
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Authentication: uses DefaultAzureCredential (Azure CLI, managed identity, etc.)
# No API keys needed — run `az login` locally
